# Demo Artifact

This folder holds the sample files pushed to Harbor as a single OCI artifact via ORAS.

- `hello.txt`: small text payload for the demo.
- `metadata.json`: simple metadata describing the artifact.
- `README.md`: this description file.

You can add more files if needed; all contents in this folder are bundled together during `oras push`.
